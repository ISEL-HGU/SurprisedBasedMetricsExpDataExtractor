        if(textIsEmptyOrNull()){
            return null;
        }
        if(textIsEmptyOrNull()){
            return null;
        }
        if(textIsEmptyOrNull()){
            return null;
        }
        if(textIsEmptyOrNull()){
            return null;
        }
        if(textIsEmptyOrNull()){
            return null;
        }
        if(textIsEmptyOrNull()){
            return null;
        }
    private boolean textIsEmptyOrNull() {
        return text() == null || text().equals("");
    }

