        return left && Boolean.TRUE.equals(right);
        return left || Boolean.TRUE.equals(right);
        return !left || Boolean.TRUE.equals(right);
        return left ^ Boolean.TRUE.equals(right);
