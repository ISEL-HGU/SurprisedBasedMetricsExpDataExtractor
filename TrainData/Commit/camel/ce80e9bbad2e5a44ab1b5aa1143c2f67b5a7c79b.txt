
        ReactiveStreamsConsumer consumer;
            consumer = this.consumer;

        if (consumer != null) {
            consumer.onError(throwable);
        }


        ReactiveStreamsConsumer consumer;
            consumer = this.consumer;

        if (consumer != null) {
            consumer.onComplete();
        }
