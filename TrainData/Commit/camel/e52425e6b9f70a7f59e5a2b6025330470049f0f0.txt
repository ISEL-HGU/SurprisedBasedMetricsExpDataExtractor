        try {
            if (isConnected()) {
                session.disconnect();
            }
        } finally {
            session = null;
    }

    @Override
    public void forceDisconnect() throws GenericFileOperationFailedException {
        try {
            session.disconnect();
        } finally {
            session = null;
        }
