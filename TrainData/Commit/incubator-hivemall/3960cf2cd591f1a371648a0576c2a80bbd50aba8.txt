            if (tuples == null) {
            List<Object> keyList = tuples.getKey();
            List<Object> valueList = tuples.getValue();
            if (tuples == null) {
                return null;
            }
            @Nullable
                if (queueHandler == null) {
                    return null;
                }

